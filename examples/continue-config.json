{
  "_comment": "AgentGuard MCP Gateway config for Continue.dev. Source: https://docs.continue.dev/customize/deep-dives/mcp (verified 2026-05-05). Continue's preferred config format is YAML in newer releases; the JSON shape below works for the legacy ~/.continue/config.json file. See continue-config.md for both options. Do NOT pass --api-key '$AGENTGUARD_API_KEY' literally — Continue does not shell-expand $VAR in JSON args. Inject the key via the env block; the gateway reads AGENTGUARD_API_KEY from the environment when the --api-key flag is absent. --tenant-id selects the tenant whose policy applies; keep 'local' unless you registered another tenant on the server with `agentguard tenant put`.",

  "mcpServers": {
    "agentguard": {
      "type": "stdio",
      "command": "agentguard-mcp-gateway",
      "args": [
        "--upstream", "fs:npx -y @modelcontextprotocol/server-filesystem /tmp",
        "--upstream", "fetch:uvx mcp-server-fetch",
        "--upstream", "github:docker run -i --rm -e GITHUB_PERSONAL_ACCESS_TOKEN ghcr.io/github/github-mcp-server",
        "--guard-url", "http://127.0.0.1:8080",
        "--policy", "/etc/agentguard/policy.yaml",
        "--tenant-id", "local",
        "--policy-mode", "strict",
        "--fail-mode", "deny",
        "--log-level", "info"
      ],
      "env": {
        "AGENTGUARD_API_KEY": "set-from-shell-or-secret-store",
        "GITHUB_PERSONAL_ACCESS_TOKEN": "set-from-shell-or-secret-store"
      }
    }
  }
}
