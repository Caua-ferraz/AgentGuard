# Binaries (anchor root-only — the unanchored form matched cmd/agentguard/ as a path component)
/agentguard
*.exe
*.dll
*.so
*.dylib

# Test
*.test
coverage.out
.coverage
coverage/

# Audit/migration runtime artifacts (live next to audit.jsonl on a running server)
*.replay-checkpoint
*.v040-backup
*.overflow.jsonl

# Build
/dist/
/bin/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Audit logs (don't commit real logs)
*.jsonl
# SQLite store (v0.6) — the database plus its WAL/SHM sidecars and journal
*.db
*.db-wal
*.db-shm
*.db-journal

# Docker
docker-compose.override.yml

# Audit working dir (multi-perspective audit artifacts)
.audit/
AUDIT_REPORT.md

# Python SDK
__pycache__/
*.pyc
*.egg-info/
dist/
build/

# Node SDK (plugins/typescript/package-lock.json is committed: CI installs
# with `npm ci` and the npm publish builds exactly the tested tree)
node_modules/

# Misc
CLAUDE.md
TODO.md
.claude/

# Local failure-audit fix notes (not for publication)
local_fixes/
